# Frontend environment variables.
# Copy to .env.local and adjust for your setup. Never commit secrets.

# Base URL of the backend API (Express server on the backend team's side).
# Same-origin when empty, otherwise a full origin such as http://localhost:5000.
NEXT_PUBLIC_API_URL=http://localhost:5000